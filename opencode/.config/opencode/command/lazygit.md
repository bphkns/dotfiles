---
name: lazygit
description: Open lazygit in the current workspace.
---

Open `lazygit` in the current workspace by running:

```bash
lazygit
```

If `lazygit` is unavailable, report the command failure briefly.
